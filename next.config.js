/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
      domains: ['obs-soltau.de'],
      unoptimized: true,
    },
    
  };
  
  module.exports = nextConfig;

  